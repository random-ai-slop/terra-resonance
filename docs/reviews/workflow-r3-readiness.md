# Workflow review, round 3: protocol readiness

Reviewer: scope_review. Inputs: primary [CURRENT](../team/CURRENT.md), proposed [WORKFLOW](../team/WORKFLOW.md), and the final six scenarios in [DESIGN](../team/DESIGN.md). This is a tabletop protocol review. No fresh specialist was started, no liveness was observed and no installed artifact was executed by this review.

**Disposition: ready for coordinator activation and repository/task setup.** No blocking ambiguity remains in the six requested scenarios. This does not authorize a specialist to implement the selected feature before its concrete assignment and accepted input are recorded.

## Executable next actions and authority

| Scenario | Next action | Authority and acceptance boundary |
|---|---|---|
| 1. Wait timeout; original writer is confirmed active | Read the current task/attempt and existing thread/child/command handles. Observe the same handle again; keep its write claim and avoid redispatch. Continue only independent already assigned work meanwhile. Record a different attempt only after the workflow's quiescence or isolation/revocation conditions are met. | Primary CURRENT identifies the accepted attempt and scope. Actual thread/process tools establish liveness. A timeout does not change either authority or task identity. |
| 2. Dirty worktree; accepted units/API change arrives | Inspect checkout/status and compare the old accepted base with the incoming accepted revision in the affected contract/API paths. Preserve tracked and untracked work; report the concrete overlap and suspend dependent edits. The coordinator decides the amended accepted base/scope; the owner reconciles without destructive reset and reruns the units/API invariants and affected consumers before handoff. Unaffected work may continue within its scope. | CURRENT's explicit accepted revision, the changed scientific contract and actual worktree files govern the decision. An incidental newer root HEAD is not a substitute. Neither a clean automatic merge nor remembered test success establishes semantic compatibility. |
| 3. Passing handoff edits primary CURRENT or comes from a superseded attempt | Do not integrate it. Check task/attempt, B→H changed paths and actual primary/worktree state. If primary CURRENT was actually modified, the coordinator restores the correct operational record from its accepted checkpoint plus legitimate intervening decisions; no reviewer performs a blanket rollback. Preserve useful code/evidence for inspection without reinstating the old claim. The currently authorized owner can produce a new in-scope handoff under the accepted attempt after reconciliation. | Only the coordinator edits primary CURRENT and grants/transfers scope. Tests cannot legitimize an unauthorized write or superseded attempt. Old output remains a possible input to a later explicit decision, not an accepted implementation. |
| 4. Renderer changes; catalog bytes unchanged | Identify affected rendering code/assets/environment and invalidate the associated image evidence. Run the relevant rendering counterexample and inspect representative output, including a supported small image if layout changed. Repair, perform the final owned generation, verify artifacts against their producing inputs, then freeze the distribution input set. Build/install from that freeze; an evidence-only final build follows the documented selective checks. Retain numerical evidence whose inputs and claims are unchanged. | Source/producer identity, Scene/production inputs and actual rendered artifacts govern freshness. Catalog hash alone does not. The coordinator records the generator owner and frozen input set and accepts which evidence is reused. |
| 5. Scientific worktree has no apps/web | Run package synchronization explicitly with `--target package` from that scientific worktree if it is within the assignment. Do not run default/all/web writes or create a replacement Site directory. Handoff the source commits; after integration the coordinator synchronizes/checks the actual Site at the absolute primary path when the task requires it. | Accepted write paths and the separate Site identity in primary CURRENT govern destinations. A root scientific commit does not contain or authorize changes to the Site repository. |
| 6. Specialist receives only a role charter | Read primary CURRENT and the linked operating/design records, inspect the actual checkout, then request the concrete task tuple from the coordinator internally and stop without implementation writes. Do not select a backlog item, create another task or begin polling. After the coordinator records task/attempt, owner, accepted base, paths, dependencies and acceptance, acknowledge that tuple and proceed. | A role title provides context only. Primary CURRENT plus the matching bounded dispatch provides execution authority. No user permission question is necessary. |

## Startup is specified, not yet performed

CURRENT provides the absolute primary record/root, coordinator task identity, separate Site path/commit, current milestone, selected trial, acceptance mapping and pending setup steps. It explicitly says scientific Git and specialist worktrees do not yet exist. WORKFLOW is marked proposed and activated by the coordinator. Its descriptions of the eventual Git/worktree arrangement therefore do not conflict with the current preactivation state.

The coordinator's next sequence is executable: accept the readiness disposition, review the root ignore/staging inventory, establish the local baseline, create the two specialist contexts/worktrees, record their real identities and bounded assignments, and observe a receiving-domain continuation. The future numerical metric/API decisions remain deliberately unassigned to implementation until the development design rounds resolve them; this is not missing bootstrap information.

Native task creation may start a first turn before its returned identity can be entered in CURRENT. Scenario 6 already supplies the safe bootstrap: the initial charter allows only reading/inspection and an internal request for assignment. Record the real identity before sending implementation work. No speculative task ID, extra approval gate or new scheduler is needed.

## Protocol consistency and stopping

The replacement rule is consistent with unique ownership: an uncertain old worker may be isolated in a separate checkout only after its integration authority is explicitly revoked. Its continuing physical process does not retain a legitimate write claim. The coordinator still cannot integrate its later output as if the old attempt were current.

The three retained records have distinct jobs: current assignment/acceptance, operating rules and decision history. The selected protocol requires no additional board, standing reviewer or executable management layer. Review counts are explicit for this trial and are not imposed on every future small maintenance change. No further planning round is justified by these six scenarios.

Actual activation must still demonstrate W3/W4: real task identities, accepted inputs, preserved work and useful dependent work in a receiving context. Leave those acceptance rows pending until observed. This tabletop result closes protocol readiness only.
